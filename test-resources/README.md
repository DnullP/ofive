# Test Resources

该目录用于存放前端/集成测试资源，不属于项目文档。

- `notes/`: mock 或测试用笔记样例（例如 markdown 语法渲染、编辑器交互验证）
- `web/`: 纯前端测试页面与对应脚本（例如 mock-tauri、wysiwyg 验证页）

约定：
- 项目正式文档放在 `docs/`。
- 测试过程中产生的临时笔记、fixture 一律放在 `test-resources/`。
